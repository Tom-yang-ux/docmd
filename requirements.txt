# DocMD 高可信 Markdown 桌面工具 — 依赖清单
# 运行必需
PySide6>=6.6
PyMuPDF>=1.24        # PDF 文本提取 + 按页渲染图片
python-docx>=1.1     # Word 文本提取
openpyxl>=3.1        # Excel 文本提取
python-pptx>=0.6.23  # PPT 文本提取
Pillow>=10.0         # 图像预处理与增强
requests>=2.31       # AI 提问（OpenAI 兼容接口）

# 可选：视觉识别（未安装时程序仍可用，走文本提取或模拟识别）
# PaddleOCR-VL 文档解析（完整布局/表格/公式/Markdown 管线；模型权重首次运行下载）
# paddleocr>=3.3
# paddlepaddle>=3.0
# paddlex>=3.0

# 可选：打包 EXE
pyinstaller>=6.0
